package com.joshua.lintCode;

import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.*;

/**
 * Created by joshua on 2017/8/25.
 */
public class DigitCountsTest {
    @Test
    public void digitCounts() throws Exception {
//        assertThat(new DigitCounts().digitCounts(0,0),is(1));
//        assertThat(new DigitCounts().digitCounts(1,100),is(21));
    }

}